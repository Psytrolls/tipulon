# טיפולון – מערכת דיגיטלית לניהול טיפול מונע באוטובוסים

מערכת מלאה ורספונסיבית (Mobile-First) בעברית מלאה (RTL) לניהול, ביצוע ותיעוד של טיפולים מונעים בציוד המותקן באוטובוסים, בהתאם למסמך האפיון המלא (גרסה 1.0).

---

## 🚀 הוראות הפעלה מהירות

### דרישות קדם
- Node.js גרסה 20 ומעלה (הפרויקט עושה שימוש ב-`node:sqlite` המובנה ב-Node).

### התקנה והרצה
1. התקנת תלויות (אם טרם הותקנו):
```bash
npm install
```

2. הרצת המערכת (שרת + ממשק משתמש):
```bash
npm run dev
```

3. פתיחת הדפדפן בכתובת:
```
http://localhost:3000
```

---

## 🔑 משתמשי מערכת מוגדרים מראש (לבדיקה והדגמה)

| תפקיד | שם | טלפון | קוד PIN | הרשאות |
| :--- | :--- | :--- | :--- | :--- |
| **מנהל מערכת** | מנהל מערכת | `0501234567` | `1234` | לוח בקרה, ניהול מוצרים, משתמשים, תור המשך טיפול, היסטוריה וייצוא לאקסל |
| **טכנאי שטח** | ישראל ישראלי | `0521234567` | `1234` | איתור אוטובוס, צילום לוחית, צ'ק-ליסט מכשירים, סיכום וסגירת דוח |

*(במסך ההתחברות קיימים גם כפתורי מילוי מהיר לדמו)*

---

## 🚌 תרחישי בדיקה עיקריים

### 1. בדיקת חסימת טיפול כפול (חוק עסקי סעיף 5.1 & 8.1)
- התחבר כטכנאי (`0521234567` / `1234`).
- חפש אוטובוס מספר: `1234567` (מוגדר לו טיפול עתידי בתוקף).
- המערכת מציגה הודעה: **"אין צורך בביצוע טיפול מונע לאוטובוס זה!"** וחוסמת את המעבר להמשך הטיפול.

### 2. ביצוע טיפול מונע מלא
- חפש אוטובוס מספר: `9876543` (או הזן מספר אוטובוס חדש).
- צרף צילום (אופציונלי, עד 4MB).
- בחר כמות מכשירים לבדיקה (בין 3 ל-12).
- מלא את פרטי כל מכשיר: בחר סוג מוצר מתוך הקטלוג הפעיל, הזן מספר סידורי, וסמן תקין/לא תקין.
- הזן סיכום טיפול ובחר: "הכול תקין באוטובוס" או "נדרש המשך טיפול של הלקוח".
- עבור על מסך הסקירה ואשר שמירת דוח סופי.

### 3. תור המשך טיפול ולוח בקרה למנהל
- התחבר כמנהל (`0501234567` / `1234`).
- צפה במוני ה-KPI: טיפולים היום, נדרש טיפול, המשך טיפול, ובאיחור.
- פתח את לשונית **תור המשך טיפול** לניהול אוטובוסים עם תקלות פתוחות וסגירתן.
- פתח את **דוחות והיסטוריה** להורדת קובץ Excel (עם קידוד UTF-8 מלא לעברית).
- נהל מוצרים ומשתמשים מהתפריט העליון.

---

## 🛠️ ארכיטקטורה טכנולוגית

- **Frontend**: React 18, Tailwind CSS (RTL מלא, תמיכה במסכי מובייל ודסקטופ), Lucide Icons, עיצוב תואם למפרט.
- **Backend**: Express.js REST API, מנגנון העלאת תמונות Multer מאובטח.
- **Database**: מסד נתונים SQLite מתמשך בקובץ מקומי (`data/tipulon.db`) באמצעות מודול `node:sqlite`.
- **אבטחה**: גיבוב PIN באמצעות PBKDF2 עם Salt ייחודי, ניהול סשנים בעוגיית HttpOnly, ואימות הרשאות בצד השרת.
